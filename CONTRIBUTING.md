# Contributing

Keep changes focused and include tests for selector or locator behavior when practical.

Before opening a pull request run:

```bash
npm run check
```

Do not add telemetry or remote network calls without a clearly documented, opt-in design discussion.
